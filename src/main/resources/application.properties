# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5433/CoPos
spring.datasource.username=Gr1zBear
spring.datasource.password=qwerty
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# Multipart file upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email/SMTP Configuration - Yandex
spring.mail.host=smtp.yandex.ru
spring.mail.port=587
spring.mail.username=coactivity@yandex.com
spring.mail.password=wmxocsknrvoyeaeh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.yandex.ru
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.transport.protocol=smtp

# Password Reset Configuration
app.password-reset.token-expiration-hours=24
app.password-reset.base-url=http://localhost:3000

# Email Verification Configuration
app.email-verification.token-expiration-hours=24
app.email-verification.base-url=http://localhost:3000

# Application Name for 2FA
app.name=CoActivity

# Application Mail From Address
app.mail.from=coactivity@yandex.com

# Recommendation API Configuration
app.recommendation.api.url=http://localhost:8000
app.recommendation.api.timeout=5000
